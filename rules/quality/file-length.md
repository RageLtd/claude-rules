# File Length

- Keep files under 500 lines. If a file exceeds this, split it into focused modules.
- When adding code to a file already near the limit, extract related functionality first.
- Prefer many small, single-purpose files over few large ones.
